// 体重（kg）
let weight = 68;

// 身長（m）
let height = 1.7;

// BMIの計算式：体重 ÷ (身長 × 身長)
let bmi = weight / (height * height);

// 結果をコンソールに出力
console.log("体重: " + weight + "kg");
console.log("身長: " + height + "m");
console.log("BMI: " + bmi);
